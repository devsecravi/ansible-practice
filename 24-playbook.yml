- name: condition
  hosts:  local
  connection: local
  vars: 
    NUMBER_OF_SESSIONS:  7
  tasks:  
  - name: condition for the loop
    ansible.builtin.debug:
      msg: "Yes, more than 5 sessions"
    when: NUMBER_OF_SESSIONS > 5