- name: install package
  hosts: local
  connection: local
  tasks:
  - name: install package
    ansible.builtin.package:
      name: "{{ name }}"
      state: present
    loop: 
      - mysql
      - nginx