- name: prompt level varaible
  hosts: local
  connection: local
  vars_prompt: 
   - name: "UserName"
     prompt: "please enter your name"
     privat: no
   - name: "password"
     prompt: "plase enter password"
     privat: yes
  tasks: 
   - name: print prompt level varaible
     ansible.builtin.debug:
     msg: "username: {{ UserName }} paasswod: {{ password }}"