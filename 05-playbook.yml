- name: practice
  hosts: local
  connection: local
  filevars:
    filesread.yml
  tasks:
    - name: print the another example
      ansible.builtin.debug:
        msg: "course is: {{ COURSE }} duration is: {{ DURATION }}"